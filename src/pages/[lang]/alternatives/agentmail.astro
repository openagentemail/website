---
import AgentmailPage from '../../../layouts/AgentmailPage.astro';
import { LOCALES } from '../../../i18n/config.js';

export function getStaticPaths() {
  return LOCALES.map((lang) => ({
    params: { lang },
  }));
}

const { lang } = Astro.params;
---

<AgentmailPage locale={lang} />
