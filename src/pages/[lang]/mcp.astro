---
import McpPage from '../../layouts/McpPage.astro';
import { LOCALES } from '../../i18n/config.js';

export function getStaticPaths() {
  return LOCALES.map((lang) => ({
    params: { lang },
  }));
}

const { lang } = Astro.params;
---

<McpPage locale={lang} />
