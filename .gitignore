# dependencies
node_modules/

# build output (OpenShip rebuilds on every deploy)
dist/
.astro/

# env / secrets
.env
.env.*

# misc
.DS_Store
npm-debug.log*

# agent battle worktrees (local only, never committed)
.arena/

# local mimosa scan output
.mimosa/
